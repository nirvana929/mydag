# mycallypro与dag_describe集成 - 快速开始

## 📦 功能概述

本次更新实现了mycallypro与dag_describe的完整集成，支持：

✅ 自动生成circle.txt配置文件（包含互斥锁和信号量信息）  
✅ 新的目录结构：分离"配置文件"和"中间结果"  
✅ 从expand文件提取5列完整信息（节点名、类型、编号、行号、文件名）  
✅ 命令行参数扩展，使用更灵活  
✅ 完全向后兼容，不影响现有功能  

## 🚀 快速开始

### 1. 基本使用

```bash
# 生成DOT图和circle.txt配置
python3 -m mycallypro input.expand \
    --export-txt output/circle.txt \
    --output-base output/
```

### 2. 目录结构

运行后会自动创建以下结构：

```
output/
├── 配置文件/           # 给dag_describe使用
│   └── basename/
│       ├── dag.dot
│       └── circle.txt  # （如果使用--export-txt）
│
└── 中间结果/           # 调试和开发用
    └── basename/
        ├── dag.dot
        └── debug/
            ├── *_control_prefix.json
            ├── *_post_parse.json
            └── *_post_callee_info.json
```

### 3. 与dag_describe集成

```bash
# Step 1: 生成配置
python3 -m mycallypro main.c.233r.expand \
    --export-txt config/circle.txt \
    --output-base config/

# Step 2: 启动dag_describe
cd test && python3 ../cally/dag_describe.py

# Step 3: 在GUI中选择配置文件夹
# 点击"选择配置文件"，选择：config/配置文件/main/
```

## 📋 命令行参数

### 新增参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `--export-txt PATH` | 导出circle.txt到指定路径 | `--export-txt output/circle.txt` |
| `--source-file PATH` | 源代码文件路径（可选） | `--source-file main.c` |
| `--output-base PATH` | 输出基础目录 | `--output-base output/` |

### 原有参数（仍然支持）

| 参数 | 说明 |
|------|------|
| `--caller FUNC` | 生成caller调用图 |
| `--callee FUNC` | 生成callee调用图 |
| `--threads-only` | 只显示线程边，不显示条件节点 |
| `--conditions-only` | 只显示条件节点 |
| `--debug` | 开启调试模式 |

## 📝 circle.txt格式说明

生成的circle.txt包含5列信息：

```txt
互斥量
节点名                            类型   编号   行号  文件名
main/while/pthread_mutex_lock5   mutex  lock1  31    simpletest.c
main/while/pthread_mutex_unlock7 mutex  lock1  33    simpletest.c


信号量
worker/while/sem_post5           sem    sem1   17    simpletest.c
main/while/sem_wait4             sem    sem1   33    simpletest.c
```

### 字段说明

1. **节点名**: 包含函数前缀和控制流上下文（if/while/switch）
2. **类型**: `mutex` 或 `sem`
3. **编号**: 唯一标识符，如 `lock1`, `sem1`
4. **行号**: 源代码行号（从expand文件提取）
5. **文件名**: 源代码文件名（从expand文件提取）

## 🔧 使用场景

### 场景1: 基本调用图分析

```bash
# 只生成DOT图，输出到stdout
python3 -m mycallypro input.expand > output.dot

# 使用Graphviz渲染
dot -Tpng output.dot -o output.png
```

### 场景2: 完整配置导出

```bash
# 生成DOT + circle.txt，保存到配置文件目录
python3 -m mycallypro input.expand \
    --export-txt config/circle.txt \
    --output-base config/ \
    --debug
```

### 场景3: 线程分析

```bash
# 只显示线程补边，不显示条件节点
python3 -m mycallypro input.expand \
    --threads-only \
    --output-base output/
```

### 场景4: 路径分析

```bash
# 分析特定函数的调用者
python3 -m mycallypro input.expand \
    --caller main \
    --max-depth 3 > caller.dot

# 分析特定函数的被调用者
python3 -m mycallypro input.expand \
    --callee pthread_create > callee.dot
```

## 🧪 测试验证

运行集成测试：

```bash
python3 test_integration.py
```

运行示例脚本：

```bash
bash 使用示例.sh
```

## 📚 详细文档

- **集成实现说明.md**: 完整的技术实现细节
- **ARCHITECTURE_CN.md**: mycallypro架构说明
- **两阶段渲染功能说明.md**: 线程视图和条件视图说明

## 🐛 故障排查

### 问题1: 找不到expand文件

```bash
# 确保expand文件存在
ls -lh *.expand

# 如果没有，重新编译生成
gcc -fdump-rtl-expand -g main.c -o main
```

### 问题2: circle.txt为空

**原因**: 代码中没有互斥锁或信号量API调用

**解决**: 
- 检查代码是否使用了 `pthread_mutex_*` 或 `sem_*` 函数
- 确保编译时链接了pthread库：`-lpthread`

### 问题3: 缺少源码行号信息

**原因**: 编译时没有开启调试信息

**解决**: 
```bash
# 添加-g选项重新编译
gcc -fdump-rtl-expand -g main.c -o main
```

### 问题4: 目录权限错误

**原因**: 没有写入权限

**解决**:
```bash
# 检查权限
ls -ld output/

# 创建目录并设置权限
mkdir -p output && chmod 755 output
```

## 💡 最佳实践

1. **编译选项**: 建议使用 `-g -O0` 获得最准确的调试信息
   ```bash
   gcc -fdump-rtl-expand -g -O0 main.c -lpthread -o main
   ```

2. **目录组织**: 为每个项目使用独立的output目录
   ```bash
   python3 -m mycallypro input.expand \
       --output-base ./output/project1/
   ```

3. **批处理**: 使用脚本处理多个文件
   ```bash
   for f in *.expand; do
       python3 -m mycallypro "$f" \
           --export-txt "output/${f%.expand}.txt" \
           --output-base output/
   done
   ```

4. **调试模式**: 开发时使用--debug查看详细信息
   ```bash
   python3 -m mycallypro input.expand \
       --debug \
       --output-base output/ 2>&1 | tee debug.log
   ```

## 🔗 相关链接

- [GCC RTL文档](https://gcc.gnu.org/onlinedocs/gccint/RTL.html)
- [Graphviz DOT语言](https://graphviz.org/doc/info/lang.html)
- [pthread编程指南](https://man7.org/linux/man-pages/man7/pthreads.7.html)

## 📞 支持

如遇问题，请：
1. 查看 `集成实现说明.md` 的详细说明
2. 运行 `test_integration.py` 验证环境
3. 使用 `--debug` 参数查看详细日志

---

**版本**: 1.0  
**更新日期**: 2025-10-23  
**兼容性**: Python 3.8+, GCC 8+
