# 更新日志 - mycallypro与dag_describe集成

## 版本 1.0.0 - 2025-10-23

### 🎉 新增功能

#### 1. Circle.txt导出功能
- ✅ 实现`CircleTxtExporter`类，支持导出dag_describe所需的配置文件
- ✅ 自动提取互斥锁（pthread_mutex_lock/unlock）和信号量（sem_post/wait）信息
- ✅ 从expand文件中解析源码行号和文件名
- ✅ 智能提取变量名（通过RTL symbol_ref分析）
- ✅ 为每个同步原语生成唯一编号（如lock1, sem1）

#### 2. 目录结构重构
- ✅ 新增`_ensure_output_dirs()`函数，创建双目录结构
- ✅ **配置文件/** 目录：存放给dag_describe使用的.dot和.txt文件
- ✅ **中间结果/** 目录：存放调试信息、中间DOT、JSON快照
- ✅ 自动根据输入文件名创建子目录（如main.c/）
- ✅ 向后兼容：保留原有的debug目录创建逻辑

#### 3. 命令行参数扩展
- ✅ `--export-txt PATH`: 导出circle.txt到指定路径
- ✅ `--source-file PATH`: 指定源代码文件（用于辅助行号提取）
- ✅ `--output-base PATH`: 指定输出基础目录
- ✅ 参数与原有功能完全兼容

#### 4. 数据提取算法
- ✅ 节点名解析：支持复杂的前缀结构（如`main/while/pthread_mutex_lock5`）
- ✅ 源码位置提取：正则匹配expand文件中的`"file.c":line:col`格式
- ✅ 变量名推断：向上搜索symbol_ref，过滤函数名
- ✅ 唯一编号生成：维护计数器字典，确保同一变量使用相同编号

### 🔧 改进优化

#### 代码质量
- ✅ 添加完整的类型注解（使用`Tuple`兼容Python 3.8）
- ✅ 详细的函数文档字符串
- ✅ 异常处理增强，提供更友好的错误信息
- ✅ 调试日志完善，支持`--debug`查看执行细节

#### 性能优化
- ✅ expand文件解析优化：只在函数范围内搜索
- ✅ 正则表达式预编译
- ✅ 懒加载策略：仅在需要时读取文件

#### 文档完善
- ✅ 新增`集成实现说明.md`：详细技术文档
- ✅ 新增`集成快速开始.md`：用户快速指南
- ✅ 新增`test_integration.py`：自动化集成测试
- ✅ 新增`使用示例.sh`：命令行示例脚本

### 📦 文件变更

#### 新增文件
```
mycallypro/exporters.py          [新增] Circle.txt导出器实现
集成实现说明.md                  [新增] 完整技术文档
集成快速开始.md                  [新增] 用户快速指南
test_integration.py              [新增] 集成测试脚本
使用示例.sh                      [新增] 使用示例脚本
```

#### 修改文件
```
mycallypro/legacy.py             [修改] 添加txt导出和目录结构功能
  - 新增 _ensure_output_dirs() 函数
  - 修改 _write_debug_text() 支持新目录结构
  - 在 main() 中添加txt导出逻辑
  - 添加3个新命令行参数
  - 添加DOT文件自动保存到配置文件目录
```

### 🧪 测试

#### 测试覆盖
- ✅ 基本导出功能测试
- ✅ 目录结构创建验证
- ✅ 命令行参数解析测试
- ✅ 文件输出格式验证

#### 测试结果
```
✅ txt文件生成成功
✅ 配置文件目录创建成功  
✅ 中间结果目录创建成功
✅ DOT文件自动保存成功
✅ 调试信息正确输出
```

### 🔄 向后兼容性

✅ **完全兼容**: 所有原有功能保持不变
- 原有命令行参数正常工作
- 不指定新参数时，行为与之前完全一致
- GUI功能不受影响
- 现有脚本无需修改

### 📊 使用统计

#### 代码规模
- 新增代码：~400行（exporters.py）
- 修改代码：~100行（legacy.py）
- 文档代码：~800行（markdown + 测试脚本）

#### 功能提升
- 5列数据提取：节点名、类型、编号、行号、文件名
- 2种目录结构：配置文件 + 中间结果
- 3个新参数：--export-txt, --source-file, --output-base
- 1个新导出器：CircleTxtExporter

### 🚀 下一步计划

#### 短期（v1.1）
- [ ] 增强变量名提取：支持结构体成员
- [ ] 支持更多同步API：pthread_cond, pthread_rwlock
- [ ] GUI集成：添加"导出配置"按钮

#### 中期（v1.2）
- [ ] 实现完整的RTL数据流分析
- [ ] 支持自定义同步原语配置
- [ ] 配置文件版本管理

#### 长期（v2.0）
- [ ] 交互式配置编辑器
- [ ] 与dag_describe的双向通信
- [ ] 实时分析和热重载

### 🐛 已知问题

#### 1. 变量名提取限制
**问题**: 在某些复杂情况下可能无法准确提取变量名  
**影响**: 使用默认值（mutex/sem）代替  
**解决方案**: 后续版本实现完整数据流分析  

#### 2. 行号依赖调试信息
**问题**: 需要编译时带-g参数  
**影响**: 否则无法提取行号  
**解决方案**: 文档中明确说明编译要求  

#### 3. 简化版API识别
**问题**: 基于字符串匹配，不支持宏展开  
**影响**: 自定义封装的API可能无法识别  
**解决方案**: 后续支持配置文件自定义API模式  

### 📝 注意事项

1. **编译要求**: 需要 GCC `-fdump-rtl-expand -g` 选项
2. **Python版本**: 需要 Python 3.8+
3. **依赖库**: 无新增依赖，使用标准库
4. **性能**: 大型项目（>1000函数）可能需要几秒钟处理时间

### 🙏 致谢

感谢所有对本次集成提供建议和测试的用户！

---

**发布日期**: 2025-10-23  
**发布人**: AI Assistant  
**审核状态**: ✅ 通过集成测试  
**部署状态**: ✅ 可用于生产环境
