# mycallypro与dag_describe集成实现说明

## ✅ 已完成的功能

### 1. 导出circle.txt功能

#### 实现位置
- **文件**: `mycallypro/exporters.py`
- **主要类**: `CircleTxtExporter`
- **便捷函数**: `export_circle_txt()`

#### 功能特性
✅ 从RTL expand文件中提取互斥锁和信号量信息  
✅ 解析节点名，提取前缀、API名称和序号  
✅ 从expand文件中提取源码行号和文件名  
✅ 尝试提取变量名（从expand文件的symbol_ref）  
✅ 为每个同步原语生成唯一编号  
✅ 生成标准格式的txt文件供dag_describe使用  

#### 输出格式
```txt
互斥量
main/while/pthread_mutex_lock5 mutex lock1 31 simpletest.c
main/while/pthread_mutex_unlock7 mutex lock1 33 simpletest.c


信号量
worker/while/sem_post5 sem sem1 17 simpletest.c
main/while/sem_wait4 sem sem1 33 simpletest.c
```

### 2. 目录结构重构

#### 新目录结构
```
项目根目录/
├── 配置文件/           # 给dag_describe使用的配置
│   ├── dag1/
│   │   ├── dag.dot
│   │   └── circle.txt
│   ├── dag2/
│   │   ├── dag.dot
│   │   └── circle.txt
│   └── ...
│
├── 中间结果/           # 开发调试用的中间文件
│   ├── dag1/
│   │   ├── dag.dot
│   │   ├── dag_full.dot
│   │   └── debug/
│   │       ├── 20251023_xxx_control_prefix.json
│   │       ├── 20251023_xxx_post_parse.json
│   │       └── ...
│   └── ...
```

#### 实现函数
- `_ensure_output_dirs(config)`: 创建配置文件和中间结果目录
- `_ensure_debug_dir(config)`: 创建debug目录（向后兼容）

### 3. 命令行参数扩展

#### 新增参数

**`--export-txt PATH`**
- 导出circle.txt配置文件到指定路径
- 示例: `--export-txt output/circle.txt`

**`--source-file PATH`**
- 指定源代码文件路径（用于辅助提取行号）
- 示例: `--source-file main.c`

**`--output-base PATH`**
- 指定输出基础目录（同时创建"配置文件"和"中间结果"两个子目录）
- 示例: `--output-base /path/to/output`

### 4. 数据提取逻辑

#### 五列数据提取

| 列名 | 说明 | 数据源 | 实现方法 |
|------|------|--------|----------|
| **节点名** | 如 `main/while/pthread_mutex_lock5` | functions['mycalls'] | 直接获取 |
| **变量类型** | `mutex` 或 `sem` | API名称判断 | `_is_mutex_api()` / `_is_sem_api()` |
| **编号名** | 如 `lock1`, `sem1` | 变量名 + 计数器 | `_generate_unique_id()` |
| **源代码行数** | 如 `31` | expand文件解析 | `_extract_source_location()` |
| **源代码文件** | 如 `simpletest.c` | expand文件解析 | `_extract_source_location()` |

#### 核心算法

**1. 解析节点名** (`_parse_node_name`)
```python
"main/while/pthread_mutex_lock5" 
→ {
    'prefix': 'main/while',
    'api': 'pthread_mutex_lock',
    'seq': 5
}
```

**2. 提取源码位置** (`_extract_source_location`)
- 在expand文件中定位函数
- 查找包含API调用和源码位置的行
- 正则提取：`"simpletest.c":31:5`

**3. 提取变量名** (`_extract_variable_name`)
- 在API调用附近向上搜索symbol_ref
- 过滤函数名，保留变量名

**4. 生成唯一编号** (`_generate_unique_id`)
- 维护计数器字典
- 格式：`{category}_{var_name}` → 编号

## 📝 使用示例

### 命令行使用

```bash
# 1. 基本用法：生成DOT并导出txt
python3 -m mycallypro input.expand \
    --export-txt output/circle.txt \
    --output-base output/

# 2. 带源文件信息
python3 -m mycallypro input.expand \
    --export-txt output/circle.txt \
    --source-file input.c \
    --output-base output/

# 3. 只生成线程图（不含条件节点）
python3 -m mycallypro input.expand \
    --threads-only \
    --export-txt output/circle.txt \
    --output-base output/

# 4. 开启调试模式
python3 -m mycallypro input.expand \
    --export-txt output/circle.txt \
    --output-base output/ \
    --debug
```

### Python API使用

```python
from pathlib import Path
from mycallypro.exporters import export_circle_txt

# 假设已经运行过mycallypro解析，获得functions字典
functions = {...}  # 从legacy.py的解析结果

# 导出txt文件
export_circle_txt(
    functions=functions,
    expand_file=Path("input.expand"),
    output_path=Path("output/circle.txt"),
    source_file=Path("input.c")  # 可选
)
```

## 🔄 与dag_describe的集成流程

```
┌─────────────┐
│  源代码.c   │
└──────┬──────┘
       │ gcc -fdump-rtl-expand
       ↓
┌─────────────────┐
│ .c.233r.expand  │
└────────┬────────┘
         │
         │ mycallypro
         │ --export-txt circle.txt
         │ --output-base output/
         ↓
┌─────────────────────┐
│   output/配置文件/  │
│   ├── dag.dot       │
│   └── circle.txt    │
└────────┬────────────┘
         │
         │ dag_describe读取
         ↓
┌─────────────────────┐
│  dag_describe GUI   │
│  - 原始图           │
│  - 互斥锁图         │
│  - 信号量图         │
│  - Tarjan分析       │
└─────────────────────┘
```

## 🔧 技术细节

### RTL解析

**expand文件格式示例：**
```plaintext
;; Function main (main, funcdef_no=1, ...)

(call_insn 42 41 43 
  (call 
    (mem:QI 
      (symbol_ref:DI ("pthread_mutex_lock"))
    )
    (const_int 0 [0])
  ) 
  "simpletest.c":31:5 -1
)
```

**提取逻辑：**
1. 定位函数边界：`;; Function {func_name}`
2. 查找API调用：`symbol_ref.*"pthread_mutex_lock"`
3. 提取位置信息：`"simpletest.c":31:5`

### 变量名推断

在API调用指令前向上搜索最近的symbol_ref：
```plaintext
(insn ... (symbol_ref:DI ("lock1")) ...)  ← 变量
(call_insn ... pthread_mutex_lock ...)     ← API调用
```

### 编号分配策略

- 同一变量的lock/unlock使用相同编号
- 不同变量按出现顺序递增编号
- 格式：`{变量名}{编号}`，如 `lock1`, `lock2`

## ⚠️ 已知限制

1. **变量名提取**：简化实现，可能无法在所有情况下准确提取变量名
   - 当前策略：向上搜索symbol_ref
   - 限制：复杂表达式或寄存器变量可能失败
   - 后备方案：使用默认值 `mutex` 或 `sem`

2. **源码行号**：依赖expand文件中的调试信息
   - 需要编译时带 `-g` 选项
   - 某些优化可能影响行号准确性

3. **API识别**：基于字符串匹配
   - 当前支持：`pthread_mutex_lock/unlock`, `sem_post/wait`
   - 扩展：修改 `_is_mutex_api()` 和 `_is_sem_api()`

## 🚀 后续扩展建议

### 1. 增强变量名提取
- 实现完整的RTL数据流分析
- 支持寄存器追踪
- 处理结构体成员访问

### 2. 支持更多同步原语
- `pthread_cond_wait/signal`
- `pthread_rwlock_rdlock/wrlock`
- 自定义同步API

### 3. GUI集成
- 在gui.py中添加"导出配置"按钮
- 自动调用dag_describe查看结果

### 4. 配置文件管理
- 实现配置文件的版本管理
- 支持多配置并存
- 提供配置文件清理工具

## 📊 测试结果

### 测试环境
- Python: 3.x
- 测试文件: `produce5/main.c.233r.expand`

### 测试输出
```
✅ 命令执行成功
✅ txt文件已生成
✅ 配置文件目录已创建: output/配置文件/main.c
✅ 中间结果目录已创建: output/中间结果/main.c
✅ DOT文件已保存
```

### 目录结构验证
```
test_output/
├── 配置文件/
│   └── main.c/
│       └── dag.dot
├── 中间结果/
│   └── main.c/
│       └── debug/
│           ├── *_control_prefix.json
│           ├── *_post_parse.json
│           └── *_post_callee_info.json
└── test_circle.txt
```

## 📚 相关文件

- `mycallypro/exporters.py` - 导出器实现
- `mycallypro/legacy.py` - 命令行和主逻辑
- `mycallypro/cli.py` - CLI入口
- `test_integration.py` - 集成测试脚本

## ✨ 总结

本次实现完成了mycallypro与dag_describe的无缝集成，主要成果：

1. ✅ **导出功能**：实现circle.txt格式导出，包含5列完整信息
2. ✅ **目录结构**：分离"配置文件"和"中间结果"，关注点明确
3. ✅ **命令行扩展**：新增3个参数，使用灵活
4. ✅ **数据提取**：从源码、expand、内部结构三方提取信息
5. ✅ **向后兼容**：保持原有功能不变，平滑升级

现在用户可以一键生成dag_describe所需的完整配置文件！
