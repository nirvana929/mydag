1、函数调用图，展示不同函数的调用关系
2、给每次调用函数都添加一个编号，这样就可以生成有时间顺序的函数调用图
3、处理pthread_create和pthread_join，这样就可以把不同线程整合进来
4、处理if、while、switch
4.1、if节点，生成一个true节点和false节点，true节点后面加入的是结果为true所执行的节点，在节点中标注if关键词，false节点后面加入的是，不进入if所执行的节点，这两个节点也需要编号，再将true的最后一个节点连接上false的下一个节点
4、2while节点，在节点中标注while关键词
4、3每一种情况都生成一个case节点，再添加default节点