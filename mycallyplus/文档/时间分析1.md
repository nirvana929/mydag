插桩方式与编译环境：目标编译器/平台？是直接修改 C 源（在函数入口/出口加计时代码），还是生成额外的 wrapper/宏文件？可否依赖 C++ 标准库或需纯 C？
计时手段：用什么 API（如 clock_gettime(CLOCK_MONOTONIC)、gettimeofday、rdtsc 等）？是否需要跨平台兼容或只支持特定环境？
输出/收集方式：时间数据如何记录？打印日志、写文件、或累积成结构在程序结束时汇总？格式有偏好吗？
插桩粒度：mycalls_meta 中的哪些函数需要插桩？全部、只对 extern==1，还是按用户勾选？
函数特性：需要处理递归/重入/多线程吗？是否要统计调用次数、平均/最大/最小耗时？
集成方式：时间分析是否作为 GUI 新按钮，还是 CLI 子命令？输出文件存放在哪个目录（沿用中间结果/<basename>/时间分析/…？）
构建/运行流程：Mycallyplus 目前只生成图，是否要扩展到自动编译/运行被插桩的程序？还是只生成插桩后的源文件供用户自行编译运行？


1、先将当前选中的C语言文件复制一份，进行相应的命名，然后进行代码插桩，
2、不需要挂平台兼容，只针对当前环境