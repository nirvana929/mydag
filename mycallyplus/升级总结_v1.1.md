# Mycallyplus v1.1 升级总结

**升级日期**: 2025-10-30  
**升级类型**: 目录结构优化 - 完全独立化

---

## ✅ 升级完成确认

### 已修改的文件

1. **文档文件 (3个)**
   - ✅ `FULL_FEATURE_GUIDE_CN.md` - 更新所有 base 路径说明
   - ✅ `UPGRADE_v1.1_独立化.md` - 新增升级文档
   - ✅ `升级总结_v1.1.md` - 本文件

2. **代码文件 (3个)**
   - ✅ `ui/gui_v3.py` - Line 91，修改 base_dir 路径
   - ✅ `ui/gui.py` - Line 118 及多处注释，修改输出路径
   - ✅ `generation/legacy.py` - Line 1486，修改默认 output_base

---

## 🎯 核心变更

### 变更前
```python
# GUI 初始化
self.base_dir = Path(__file__).resolve().parent.parent.parent / "mycallypro"

# Legacy 默认路径
config.output_base = str(Path(__file__).parent)  # → mycallyplus/generation/
```

**输出目录**: `mycallypro/配置文件/`, `mycallypro/中间结果/`, `mycallypro/dag图/`

### 变更后
```python
# GUI 初始化
self.base_dir = Path(__file__).resolve().parent.parent  # → mycallyplus/

# Legacy 默认路径
config.output_base = str(Path(__file__).parent.parent)  # → mycallyplus/
```

**输出目录**: `mycallyplus/配置文件/`, `mycallyplus/中间结果/`, `mycallyplus/dag图/`

---

## 📂 目录结构对比

### 升级前
```
cally/
├── mycallypro/          ← 所有输出在这里
│   ├── 配置文件/
│   ├── 中间结果/
│   └── dag图/
└── mycallyplus/         ← 只有源代码
    ├── core/
    ├── generation/
    ├── ui/
    └── visualization/
```

### 升级后
```
cally/
├── mycallypro/          ← 独立项目，保持不变
│   └── ...
└── mycallyplus/         ← 完全独立，包含所有输出
    ├── core/
    ├── generation/
    ├── ui/
    ├── visualization/
    ├── 配置文件/       ← 新增
    ├── 中间结果/       ← 新增
    ├── dag图/          ← 新增
    └── 源文件/         ← 新增
```

---

## 🔍 修改细节

### 1. gui_v3.py

**位置**: Line 91

```diff
- self.base_dir = Path(__file__).resolve().parent.parent.parent / "mycallypro"
+ self.base_dir = Path(__file__).resolve().parent.parent
```

**影响**: 
- 所有 GUI v3 操作的输出路径
- 7个主功能按钮的文件保存位置

---

### 2. gui.py

**位置**: Line 118

```diff
- self.base_dir = Path(__file__).resolve().parent.parent.parent / "mycallypro"
+ self.base_dir = Path(__file__).resolve().parent.parent
```

**位置**: 多处注释更新

```diff
- # 调用 legacy，指定output_base为mycallypro目录
+ # 调用 legacy，指定output_base为mycallyplus目录

- # legacy 会生成在 mycallypro/配置文件/<basename>/
+ # legacy 会生成在 mycallyplus/配置文件/<basename>/
```

**影响**:
- 10个主功能按钮的输出路径
- 生成DAG、条件节点、配置文件的保存位置

---

### 3. legacy.py

**位置**: Line 1486

```diff
- # 如果没有指定output_base，默认使用mycallypro目录
+ # 如果没有指定output_base，默认使用mycallyplus目录
  if not hasattr(config, 'output_base') or not config.output_base:
-     config.output_base = str(Path(__file__).parent)
+     config.output_base = str(Path(__file__).parent.parent)
```

**影响**:
- CLI 命令在不指定 `--output-base` 时的默认输出位置
- 从 `mycallyplus/generation/` 改为 `mycallyplus/`

---

### 4. FULL_FEATURE_GUIDE_CN.md

**修改位置**: 3处

1. **§1 总体架构 - base 的取值**
```diff
- GUI/推荐：`mycallypro/`（GUI 在调用 legacy 时通过 `--output-base` 指定）
+ GUI/推荐：`mycallyplus/`（GUI 在调用 legacy 时通过 `--output-base` 指定）
```

2. **§2 CLI 子命令 - --output-base 说明**
```diff
- `--output-base <DIR>`：指定输出根目录（推荐 `mycallypro/`）
+ `--output-base <DIR>`：指定输出根目录（推荐 `mycallyplus/`）
```

3. **§7 输出文件清单与位置**
```diff
- 以 `<base>=mycallypro/`、`<b>=<basename>` 为例（GUI/推荐模式）：
+ 以 `<base>=mycallyplus/`、`<b>=<basename>` 为例（GUI/推荐模式）：
```

---

## 🧪 测试验证

### GUI 测试

```bash
cd /home/chove/桌面/cally
python -m mycallyplus

# 操作：选择源文件 → 生成dag图
# 预期：文件生成在 mycallyplus/配置文件/<basename>/
```

### CLI 测试

```bash
cd /home/chove/桌面/cally

# 测试1: 默认输出
python -m mycallyplus generate test.c.233r.expand
# 预期：输出到 mycallyplus/配置文件/test/

# 测试2: 显式指定
python -m mycallyplus generate test.c.233r.expand --output-base mycallyplus/
# 预期：输出到 mycallyplus/配置文件/test/

# 测试3: 查看器
python -m mycallyplus describe --open mycallyplus/配置文件/test/
# 预期：正常加载并显示
```

---

## 📊 影响分析

### 正面影响 ✅

1. **完全独立**
   - mycallyplus 不再依赖 mycallypro 目录
   - 便于单独分发和部署

2. **目录清晰**
   - 所有输出集中在项目内部
   - 便于管理和清理

3. **避免混淆**
   - 用户不会误以为需要 mycallypro 项目
   - 项目边界更清晰

### 兼容性 ✅

1. **配置文件格式**
   - ✅ 完全兼容，无需修改
   - ✅ circle.txt 格式不变
   - ✅ DOT 文件格式不变

2. **功能行为**
   - ✅ 所有功能保持不变
   - ✅ 只是输出路径改变
   - ✅ 用户体验一致

3. **向后兼容**
   - ✅ 旧的配置文件可直接使用
   - ✅ 命令行参数不变
   - ⚠️ 输出位置改变（需要用户注意）

---

## 🚀 使用建议

### GUI 用户（推荐）

```bash
# 直接使用，无需任何修改
cd /home/chove/桌面/cally
python -m mycallyplus
```

**优点**:
- 自动使用正确的输出路径
- 无需手动指定任何参数
- 最简单、最安全的使用方式

### CLI 用户

```bash
# 方式1: 使用默认路径（推荐）
python -m mycallyplus generate file.expand

# 方式2: 显式指定（更明确）
python -m mycallyplus generate file.expand --output-base mycallyplus/

# 方式3: 自定义位置（高级）
python -m mycallyplus generate file.expand --output-base /custom/path/
```

---

## 📋 检查清单

升级完成后，请确认：

- [ ] 文档已更新（3个文件）
- [ ] 代码已修改（3个文件）
- [ ] GUI 测试通过
- [ ] CLI 测试通过
- [ ] 输出文件在正确位置
- [ ] 旧的 mycallypro 目录保持不变

---

## 🔜 后续工作

### 可选清理任务

1. **清理旧输出**（如果不再需要）
```bash
cd /home/chove/桌面/cally
rm -rf mycallypro/配置文件
rm -rf mycallypro/中间结果
rm -rf mycallypro/dag图
```

2. **更新其他文档**（如果有）
- 检查是否有其他文档提到输出路径
- 更新示例代码中的路径

3. **通知用户**（如果需要）
- 更新 README 的最后修改日期
- 添加版本更新日志

---

## 📝 版本信息

- **当前版本**: v1.1
- **上一版本**: v1.0
- **发布日期**: 2025-10-30
- **升级类型**: 目录结构优化
- **破坏性变更**: 无（仅输出路径变化）

---

## ✨ 总结

### 升级内容

- ✅ 修改了 3 个代码文件
- ✅ 更新了 1 个技术文档
- ✅ 新增了 2 个说明文档
- ✅ 实现了完全独立化

### 核心改进

1. **独立性**: mycallyplus 现在是完全独立的项目
2. **清晰度**: 输出路径更直观，不会产生混淆
3. **可维护性**: 便于项目管理和部署

### 用户影响

- **GUI 用户**: 无影响，自动使用新路径
- **CLI 用户**: 输出路径改变，但可通过 `--output-base` 控制
- **开发者**: 项目结构更清晰，便于维护

---

**升级完成！** 🎉

如有任何问题，请参考：
- `UPGRADE_v1.1_独立化.md` - 详细升级说明
- `FULL_FEATURE_GUIDE_CN.md` - 完整功能指南
- `README.md` - 项目概述
