# 当前开发需求（S02）

## 背景
- Level-2 分块结果与最新规则不一致，尤其是合并规则目前不是按“有效代码行相邻”执行。
- 需要把 Level-2 分块严格对齐到 `level2级分块.md`，并完成 zhang1 的验证闭环。

## 本轮目标
- [x] 在 `segment_dag_level2.py` 实现“有效代码行相邻”合并规则（跳过空行与注释）。
- [x] 线程首尾补线规则按首/尾调用节点生效（main + 线程入口函数）。
- [x] 用 zhang1 重新生成 Level-2 分段产物（json/dot/png）。
- [x] 完成 S02 阶段文档沉淀。

## 明确不做
- 不处理“同一物理行命中多个同步原语”的冲突场景。
- 不扩展宏封装 API 识别（仅 pthread/sem 原生调用名）。
- 不改动 Level-1 实现与其输出结构。

## 约束
- 依然使用 `functions_full.json`、`functions_ranges.json`、`mycalls_meta_internal.json`、`circle.txt`。
- 线程首尾调用顺序以 `mycalls_meta_internal.json` 的有序键序列为准。
- 先保证规则正确，再考虑性能优化。

## 验收标准
- [x] create/sem_post 后切；join/sem_wait 前切；lock 前切；unlock 后切。
- [x] create/sem_post 合并：上一条有效代码为 unlock 时删除 unlock 切线。
- [x] join/sem_wait 合并：下一条有效代码为 lock 时删除 lock 前切线。
- [x] 空行与注释不会阻断“上一条/下一条有效代码”判断。
- [x] `zhang1` 的 Level-2 产物可完整生成且 DAG 基本结构有效。

## 未决问题
- 当前“入口补线”在闭区间分段模型中主要体现在规则记录，不会额外产生空段；后续是否需要在可视化中显式标注入口切线。

## 备注
- 最后更新时间：2026-02-06
