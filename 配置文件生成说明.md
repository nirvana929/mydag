# 配置文件生成功能说明

## 功能概述

点击GUI的"生成配置文件"按钮后，系统会自动生成所有配置文件并按照规范的目录结构存储，方便与dag_describe工具集成使用。

## 目录结构设计

```
配置文件/
└── <程序名>/
    ├── circle.txt              # 配置文件（根目录，供dag_describe使用）
    ├── source/                 # 源代码文件
    │   └── *.c
    ├── expand/                 # GCC expand文件
    │   └── *.c.233r.expand
    ├── dot/                    # DOT图文件
    │   ├── dag.dot             # 线程视图
    │   └── dag_full.dot        # 完整视图（包含条件节点）
    ├── images/                 # PNG图片
    │   ├── dag.png             # 线程视图图片
    │   └── dag_full.png        # 完整视图图片
    └── debug/                  # 调试文件
        ├── *_control_prefix.json
        ├── *_post_parse.json
        ├── *_post_callee_info.json
        └── *_threads.json
```

## 生成的文件说明

### 1. circle.txt（配置文件）
- **位置**: 配置文件根目录
- **格式**: 5列文本文件
  - 第1列: 节点路径（如 `producer/while/pthread_mutex_lock2`）
  - 第2列: 类型（`mutex`、`semaphore`、`condition`）
  - 第3列: 变量名（如 `buf_mutex1`）
  - 第4列: 行号（如 `34`）
  - 第5列: 文件名（如 `produce.c`）
- **用途**: 供dag_describe工具读取，用于环路检测和分析

### 2. source/（源代码目录）
- 存储原始C源代码文件
- 用于代码审查和问题定位

### 3. expand/（展开文件目录）
- 存储GCC生成的RTL expand文件
- 包含完整的中间代码表示

### 4. dot/（图文件目录）
- `dag.dot`: 线程视图的DOT文件
- `dag_full.dot`: 完整视图的DOT文件（包含条件节点）

### 5. images/（图片目录）
- `dag.png`: 线程视图的可视化图片
- `dag_full.png`: 完整视图的可视化图片

### 6. debug/（调试目录）
- 存储解析过程中的调试JSON文件
- 包含控制流、解析结果、函数信息等

## 使用方法

### 方法1: GUI界面（推荐）

1. 启动GUI：
   ```bash
   python3 mycallypro/gui.py
   ```

2. 选择文件：
   - 点击"选择 C 文件"选择源代码
   - 或点击"选择 expand 文件"直接选择expand文件

3. 生成配置：
   - 点击"生成配置文件"按钮
   - 系统会自动完成以下操作：
     - 创建目录结构
     - 复制源文件和expand文件
     - 生成线程视图DAG
     - 生成完整视图DAG
     - 生成circle.txt配置文件
     - 显示成功消息和文件列表

### 方法2: 命令行

```bash
python3 -m mycallypro <expand_file> \
    --export-txt circle.txt \
    --output-base <工作目录> \
    --source-file <源文件> \
    --debug
```

**参数说明**:
- `<expand_file>`: GCC生成的expand文件路径
- `--export-txt`: 指定导出circle.txt（会自动保存到配置文件目录）
- `--output-base`: 指定输出根目录（会在此目录下创建"配置文件"文件夹）
- `--source-file`: 源代码文件路径（可选）
- `--debug`: 显示详细调试信息（可选）

**示例**:
```bash
cd /home/chove/桌面/cally
python3 -m mycallypro mycallypro/test/produce/produce.c.233r.expand \
    --export-txt circle.txt \
    --output-base /home/chove/桌面/cally \
    --source-file mycallypro/中间结果文件/produce/produce.c \
    --debug
```

## 测试验证

运行测试脚本验证功能：
```bash
python3 test_config_generation.py
```

测试脚本会：
1. 清理旧的配置目录
2. 生成新的配置文件
3. 验证目录结构
4. 验证circle.txt格式
5. 显示完整的目录树

## 技术实现

### 修改的文件

1. **mycallypro/legacy.py**
   - `_ensure_output_dirs()`: 创建规范的目录结构
   - `_write_debug_text()`: 优先写入配置目录的debug子目录
   - 导出逻辑: 自动保存circle.txt到配置目录
   - 文件复制: 自动复制源文件和expand文件到对应子目录

2. **mycallypro/gui.py**
   - `generate_config_files()`: 一键生成所有配置文件
   - `_build_dag_to_config()`: 保存DOT和PNG到对应子目录
   - 文件复制: 使用shutil复制源文件和expand文件

3. **mycallypro/exporters.py**
   - `CircleTxtExporter`: 生成5列格式的circle.txt
   - 源代码行号提取
   - 同步原语识别

### 关键特性

- ✅ 自动创建规范的目录结构
- ✅ 文件按类别分类存储
- ✅ circle.txt包含完整的5列信息
- ✅ 支持GUI和命令行两种方式
- ✅ 自动复制源文件和expand文件
- ✅ 调试文件存储到debug子目录
- ✅ 生成线程视图和完整视图两种DAG

## 与dag_describe集成

生成的配置文件目录可以直接用于dag_describe工具：

1. circle.txt位于根目录，方便dag_describe读取
2. source目录包含源代码，方便代码审查
3. dot目录包含DAG文件，可以查看图结构
4. images目录包含可视化图片，方便理解

## 常见问题

### Q: 为什么source目录是空的？
A: 需要在命令行使用`--source-file`参数，或在GUI中先选择C文件。

### Q: circle.txt只有3列怎么办？
A: 确保提供了源文件路径（`--source-file`），系统需要从源文件中提取行号信息。

### Q: debug目录是空的？
A: 在命令行使用`--debug`参数会生成debug文件。GUI模式下会自动生成。

### Q: 如何生成PNG图片？
A: 需要安装Graphviz：`sudo apt-get install graphviz`

## 更新日期

2025年10月23日
